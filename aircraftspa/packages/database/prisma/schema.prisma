generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Placeholder model for initial client generation
// Will be replaced with full schema in subsequent tasks
model Placeholder {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
}
