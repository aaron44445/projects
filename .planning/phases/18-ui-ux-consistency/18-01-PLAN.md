---
phase: 18-ui-ux-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/statusColors.ts
  - packages/ui/src/components/EmptyState.tsx
  - packages/ui/src/index.ts
autonomous: true

must_haves:
  truths:
    - "Import { STATUS_COLORS, getStatusClasses } from '@/lib/statusColors' works"
    - "Import { EmptyState } from '@peacase/ui' works"
    - "StatusKey type provides IntelliSense for valid status names"
    - "EmptyState renders icon, title, description, and optional action button"
  artifacts:
    - path: "apps/web/src/lib/statusColors.ts"
      provides: "Centralized status color mappings using design tokens"
      exports: ["STATUS_COLORS", "StatusKey", "getStatusClasses"]
    - path: "packages/ui/src/components/EmptyState.tsx"
      provides: "Reusable empty state component"
      exports: ["EmptyState", "EmptyStateProps"]
  key_links:
    - from: "packages/ui/src/index.ts"
      to: "packages/ui/src/components/EmptyState.tsx"
      via: "re-export"
      pattern: "export.*EmptyState"
---

<objective>
Create foundation components for UI consistency: statusColors utility and EmptyState component.

Purpose: Establish single sources of truth for status colors (UI-02) and empty states (UI-04) before migrating existing implementations.
Output: Two new files providing the building blocks for subsequent plans.
</objective>

<execution_context>
@C:\Users\aaron\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aaron\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-ui-ux-consistency/18-RESEARCH.md
@apps/web/tailwind.config.ts (design tokens reference)
@packages/ui/src/components/Badge.tsx (CVA variant pattern)
@packages/ui/src/index.ts (export pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create statusColors utility</name>
  <files>apps/web/src/lib/statusColors.ts</files>
  <action>
Create status color utility with design system tokens:

1. Define STATUS_COLORS const object with semantic status keys:
   - confirmed/completed: bg-sage/10, text-sage-dark, border-sage/20
   - scheduled/pending: bg-soft-lavender, text-charcoal, border-lavender/20
   - in-progress: bg-lavender/20, text-lavender-dark, border-lavender/30
   - cancelled/no-show/expired: bg-rose/10, text-rose-dark, border-rose/20
   - draft: bg-charcoal/5, text-text-muted, border-charcoal/10

2. Export StatusKey type as keyof typeof STATUS_COLORS

3. Export getStatusClasses(status: StatusKey) helper that returns combined class string

4. Use `as const` for type inference

Reference the RESEARCH.md Pattern 1: Design Token Status Colors for exact implementation.
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit -p apps/web/tsconfig.json`</verify>
  <done>STATUS_COLORS, StatusKey, and getStatusClasses exported from apps/web/src/lib/statusColors.ts</done>
</task>

<task type="auto">
  <name>Task 2: Create EmptyState component</name>
  <files>packages/ui/src/components/EmptyState.tsx, packages/ui/src/index.ts</files>
  <action>
Create EmptyState component in packages/ui:

1. Define EmptyStateProps interface:
   - icon: React.ComponentType<{ size?: number; className?: string }>
   - title: string
   - description: string
   - action?: { label: string; onClick: () => void; icon?: React.ComponentType<{ size?: number }> }

2. Implement EmptyState component with:
   - Centered flex layout with py-12 px-4
   - Icon in 16x16 rounded-full bg-charcoal/5 container
   - Title as text-lg font-semibold text-charcoal
   - Description as text-text-muted max-w-sm
   - Optional action button with bg-sage text-white hover:bg-sage-dark

3. Export from packages/ui/src/index.ts: `export { EmptyState, type EmptyStateProps } from './components/EmptyState';`

Reference the RESEARCH.md Pattern 3: EmptyState Component with Semantic Props for exact implementation.
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit -p packages/ui/tsconfig.json`</verify>
  <done>EmptyState component exported from @peacase/ui package</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p apps/web/tsconfig.json` - Web app type checks
2. `npx tsc --noEmit -p packages/ui/tsconfig.json` - UI package type checks
3. Verify exports:
   - `grep -q "STATUS_COLORS" apps/web/src/lib/statusColors.ts`
   - `grep -q "EmptyState" packages/ui/src/index.ts`
</verification>

<success_criteria>
- statusColors.ts provides type-safe status color mappings using design tokens
- EmptyState component follows design system patterns
- Both files export all required types and functions
- TypeScript compilation succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-ui-ux-consistency/18-01-SUMMARY.md`
</output>
