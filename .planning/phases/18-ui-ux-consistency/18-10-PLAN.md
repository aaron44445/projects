---
phase: 18-ui-ux-consistency
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/settings/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Settings page uses bg-rose/10 instead of bg-red-50/100"
    - "Settings page uses text-rose-dark instead of text-red-500/600/700"
    - "Settings page uses border-rose/20 instead of border-red-200"
    - "No hardcoded red color patterns remain"
  artifacts:
    - path: "apps/web/src/app/settings/page.tsx"
      provides: "Settings page with rose design tokens"
      pattern: "bg-rose/10|text-rose-dark|border-rose/20"
  key_links:
    - from: "error states in settings"
      to: "rose design tokens"
      via: "Tailwind classes"
---

<objective>
Replace 45+ hardcoded red color usages with rose design tokens in settings page

Purpose: Close Gap 3 (UI-03) for settings page - largest file with error token issues
Output: Settings page using rose design tokens for all error states
</objective>

<execution_context>
@C:\Users\aaron\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aaron\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-ui-ux-consistency/18-02-SUMMARY.md

Reference the established error state pattern:
- Error: bg-rose/10 border-rose/20 text-rose-dark
- Dark mode: bg-rose/20 border-rose/30 text-rose
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace background red colors</name>
  <files>apps/web/src/app/settings/page.tsx</files>
  <action>
    Settings page (3438 lines) has 45+ red color usages. Use find/replace with patterns:

    Background colors:
    - bg-red-50 -> bg-rose/10
    - bg-red-100 -> bg-rose/10 (or bg-rose/20 for slightly stronger)
    - bg-red-500 -> bg-rose (solid background rare, check usage)
    - bg-red-600 -> bg-rose-dark

    For dark mode variants:
    - dark:bg-red-900 -> dark:bg-rose/20
    - dark:bg-red-950 -> dark:bg-rose/10

    Note: Use bulk replacement carefully. Review each instance if context differs:
    - Error messages/banners: bg-rose/10
    - Error buttons: bg-rose or bg-rose-dark
    - Error indicators: bg-rose/20
  </action>
  <verify>
    - grep "bg-red-" apps/web/src/app/settings/page.tsx returns no results
    - grep "bg-rose" apps/web/src/app/settings/page.tsx returns results
  </verify>
  <done>All bg-red-* replaced with bg-rose/* design tokens</done>
</task>

<task type="auto">
  <name>Task 2: Replace text red colors</name>
  <files>apps/web/src/app/settings/page.tsx</files>
  <action>
    Text colors:
    - text-red-400 -> text-rose
    - text-red-500 -> text-rose-dark
    - text-red-600 -> text-rose-dark
    - text-red-700 -> text-rose-dark
    - text-red-800 -> text-rose-dark

    For dark mode:
    - dark:text-red-400 -> dark:text-rose
    - dark:text-red-300 -> dark:text-rose

    Semantic usage:
    - Error text: text-rose-dark (dark:text-rose)
    - Destructive button text: text-rose-dark or text-white on rose background
    - Error icons: text-rose-dark
  </action>
  <verify>
    - grep "text-red-" apps/web/src/app/settings/page.tsx returns no results
    - grep "text-rose" apps/web/src/app/settings/page.tsx returns results
  </verify>
  <done>All text-red-* replaced with text-rose* design tokens</done>
</task>

<task type="auto">
  <name>Task 3: Replace border red colors</name>
  <files>apps/web/src/app/settings/page.tsx</files>
  <action>
    Border colors:
    - border-red-200 -> border-rose/20
    - border-red-300 -> border-rose/30
    - border-red-500 -> border-rose/50

    For dark mode:
    - dark:border-red-700 -> dark:border-rose/30
    - dark:border-red-800 -> dark:border-rose/20

    Ring colors (if present):
    - ring-red-500 -> ring-rose
    - ring-red-400 -> ring-rose/80
    - focus:ring-red-* -> focus:ring-rose/*
  </action>
  <verify>
    - grep "border-red-" apps/web/src/app/settings/page.tsx returns no results
    - grep "ring-red-" apps/web/src/app/settings/page.tsx returns no results
    - npm run typecheck passes
  </verify>
  <done>All border-red-* and ring-red-* replaced with rose design tokens</done>
</task>

</tasks>

<verification>
Run after all tasks:
```bash
# Verify no hardcoded red patterns remain
grep -E "(bg|text|border|ring)-red-" apps/web/src/app/settings/page.tsx

# Verify rose tokens present
grep -c "rose" apps/web/src/app/settings/page.tsx

# Count replacements made
echo "Approximate rose usages (should be ~45+):"
grep -c "rose" apps/web/src/app/settings/page.tsx

# TypeScript compilation
npm run typecheck
```
</verification>

<success_criteria>
- [ ] No bg-red-* patterns remain in settings page
- [ ] No text-red-* patterns remain in settings page
- [ ] No border-red-* patterns remain in settings page
- [ ] Rose design tokens used consistently
- [ ] TypeScript compiles without errors
- [ ] Commit: feat(18-10): migrate settings page error colors to rose design tokens
</success_criteria>

<output>
After completion, create `.planning/phases/18-ui-ux-consistency/18-10-SUMMARY.md`
</output>
