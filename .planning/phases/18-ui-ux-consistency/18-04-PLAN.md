---
phase: 18-ui-ux-consistency
plan: 04
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - apps/web/src/app/dashboard/page.tsx
  - apps/web/src/app/clients/page.tsx
  - apps/web/src/app/services/page.tsx
  - apps/web/src/app/staff/page.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard empty activity state uses EmptyState component"
    - "Clients page empty state uses EmptyState component"
    - "Services page empty state uses EmptyState component"
    - "Staff page empty state uses EmptyState component"
    - "All empty states have icon, title, description, and optional action"
  artifacts:
    - path: "apps/web/src/app/dashboard/page.tsx"
      provides: "Dashboard with EmptyState for activity feed"
      contains: "import { EmptyState } from '@peacase/ui'"
    - path: "apps/web/src/app/clients/page.tsx"
      provides: "Clients page with EmptyState"
      contains: "import { EmptyState } from '@peacase/ui'"
  key_links:
    - from: "apps/web/src/app/dashboard/page.tsx"
      to: "packages/ui/src/components/EmptyState.tsx"
      via: "import"
      pattern: "import.*EmptyState.*@peacase/ui"
---

<objective>
Migrate key pages to use the shared EmptyState component for consistent empty state messaging.

Purpose: Standardize empty states across the app (UI-04) with consistent styling, messaging patterns, and optional action buttons.
Output: Dashboard, clients, services, and staff pages using EmptyState component.
</objective>

<execution_context>
@C:\Users\aaron\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aaron\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-ui-ux-consistency/18-01-SUMMARY.md
@packages/ui/src/components/EmptyState.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dashboard empty activity state</name>
  <files>apps/web/src/app/dashboard/page.tsx</files>
  <action>
Update the dashboard's empty activity feed to use EmptyState:

1. Add import: `import { EmptyState } from '@peacase/ui';`

2. Find the empty activity state (around lines 970-977):
   ```tsx
   // Current:
   <div className="text-center py-12">
     <Clock className="w-16 h-16 text-charcoal/20 mx-auto mb-4" />
     <p className="text-lg text-text-muted">No activity yet</p>
     <p className="text-sm text-charcoal/40 mt-2">
       Your activity feed will show bookings, payments, and more
     </p>
   </div>
   ```

3. Replace with:
   ```tsx
   <EmptyState
     icon={Clock}
     title="No activity yet"
     description="Your activity feed will show bookings, payments, and more"
   />
   ```

Note: No action button needed for activity feed - it's informational.
  </action>
  <verify>`grep -q "import { EmptyState } from '@peacase/ui'" apps/web/src/app/dashboard/page.tsx`</verify>
  <done>Dashboard empty activity state uses EmptyState component</done>
</task>

<task type="auto">
  <name>Task 2: Update clients page empty state</name>
  <files>apps/web/src/app/clients/page.tsx</files>
  <action>
Update the clients page empty state to use EmptyState:

1. Add import: `import { EmptyState } from '@peacase/ui';`
2. Import Plus icon if not already: `import { Plus, ... } from 'lucide-react';`

3. Find the empty state (around lines 717-726):
   ```tsx
   // Current conditional empty state for search vs initial
   <div className="p-12 text-center">
     <Users className="w-12 h-12 text-charcoal/20 dark:text-white/20 mx-auto mb-4" />
     <p className="text-charcoal/60 dark:text-white/60">No clients found</p>
     <p className="text-sm text-charcoal/40 dark:text-white/40 mt-1">
       {searchQuery ? 'Try adjusting your search' : 'Add your first client to get started'}
     </p>
   </div>
   ```

4. Replace with conditional EmptyState:
   ```tsx
   <EmptyState
     icon={Users}
     title={searchQuery ? "No clients found" : "No clients yet"}
     description={searchQuery ? "Try adjusting your search or filters" : "Add your first client to get started"}
     action={!searchQuery ? {
       label: "Add Client",
       onClick: () => setShowAddModal(true),
       icon: Plus
     } : undefined}
   />
   ```

Note: Use "No clients yet" for initial state (implies can add), "No clients found" for search (implies filter active).
  </action>
  <verify>`grep -q "import { EmptyState } from '@peacase/ui'" apps/web/src/app/clients/page.tsx`</verify>
  <done>Clients page empty state uses EmptyState component with conditional action</done>
</task>

<task type="auto">
  <name>Task 3: Update services and staff page empty states</name>
  <files>apps/web/src/app/services/page.tsx, apps/web/src/app/staff/page.tsx</files>
  <action>
Update services and staff pages to use EmptyState:

**Services page (apps/web/src/app/services/page.tsx):**
1. Add import: `import { EmptyState } from '@peacase/ui';`
2. Find empty state and replace with:
   ```tsx
   <EmptyState
     icon={Scissors}  // or appropriate service icon
     title="No services yet"
     description="Add your first service to start booking appointments"
     action={{
       label: "Add Service",
       onClick: () => setShowAddModal(true),
       icon: Plus
     }}
   />
   ```

**Staff page (apps/web/src/app/staff/page.tsx):**
1. Add import: `import { EmptyState } from '@peacase/ui';`
2. Find empty state and replace with:
   ```tsx
   <EmptyState
     icon={Users}  // or appropriate staff icon
     title="No staff members yet"
     description="Add your first staff member to start scheduling"
     action={{
       label: "Add Staff",
       onClick: () => setShowAddModal(true),
       icon: Plus
     }}
   />
   ```

Note: Match existing modal trigger functions. Use appropriate icons from lucide-react already imported in each file.
  </action>
  <verify>
1. `grep -q "import { EmptyState } from '@peacase/ui'" apps/web/src/app/services/page.tsx`
2. `grep -q "import { EmptyState } from '@peacase/ui'" apps/web/src/app/staff/page.tsx`
  </verify>
  <done>Services and staff pages use EmptyState component with action buttons</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p apps/web/tsconfig.json` - TypeScript passes
2. All four pages import EmptyState from @peacase/ui
3. No ad-hoc empty state markup remains in migrated sections
4. Action buttons trigger correct modal functions
</verification>

<success_criteria>
- Dashboard, clients, services, and staff pages import EmptyState from @peacase/ui
- Empty states have consistent styling (icon, title, description)
- Initial empty states use "No X yet" messaging with action button
- Search/filter empty states use "No X found" messaging without action
- All empty state actions trigger correct modal functions
</success_criteria>

<output>
After completion, create `.planning/phases/18-ui-ux-consistency/18-04-SUMMARY.md`
</output>
