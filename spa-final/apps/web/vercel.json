{
  "buildCommand": "cd ../.. && npm install -g pnpm && pnpm install && pnpm turbo run build --filter=@peacase/web",
  "devCommand": "pnpm dev",
  "installCommand": "npm install -g pnpm && pnpm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
